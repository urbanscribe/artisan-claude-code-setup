{
  "permissions": {
    "Read": "allow",
    "Grep": "allow",
    "Glob": "allow",
    "Edit": "prompt",
    "Bash": {
      "git status": "allow",
      "git diff": "allow",
      "git log": "allow",
      "git show": "allow",
      "npm test": "prompt",
      "python -m pytest": "prompt",
      "docker compose ps": "allow",
      "docker compose logs": "allow"
    }
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'CLAUDE_DIR=\"$(while [ ! -d .claude ] && [ \"$PWD\" != / ]; do cd ..; done; if [ -d .claude ]; then echo \"$PWD/.claude\"; fi)\"; if [ -n \"$CLAUDE_DIR\" ]; then \"$CLAUDE_DIR/hooks/safety_pre_tool_wrapper.sh\"; fi'"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'CLAUDE_DIR=\"$(while [ ! -d .claude ] && [ \"$PWD\" != / ]; do cd ..; done; if [ -d .claude ]; then echo \"$PWD/.claude\"; fi)\"; if [ -n \"$CLAUDE_DIR\" ]; then \"$CLAUDE_DIR/hooks/validation_post_tool_wrapper.sh\"; fi'"
          }
        ]
      }
    ]
  }
}
